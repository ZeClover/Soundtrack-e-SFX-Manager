# Ambiente virtual Python
.venv/
venv/
env/

# Bytecode / cache
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Bancos de dados locais e logs (dados do usuário, não fazem parte do código)
data/
*.db
*.db-journal
*.db-wal
*.db-shm
logs/

# Build / empacotamento (Etapa 6, item 67) — o .spec oficial (ex.:
# RPGAudioStudio.spec) fica versionado; só a saída do build e artefatos
# gerados/derivados ficam de fora.
build/
dist/
release/
*.spec.bak
*.egg-info/
version_info.txt
rpg-audio-studio/ffmpeg/

# Editor / SO
.vscode/
.idea/
.DS_Store
Thumbs.db
